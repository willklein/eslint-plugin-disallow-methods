{
  "name": "eslint-plugin-disallow-methods",
  "version": "0.0.0",
  "description": "ESLint plugin for disallowing specified methods",
  "main": "index.js",
  "scripts": {
    "minor-release": "npm version minor && npm publish && git push --follow-tags",
    "patch-release": "npm version patch && npm publish && git push --follow-tags",
    "test": "mocha tests/rules"
  },
  "keywords": [
    "eslint",
    "eslintplugin"
  ],
  "license": "MIT",
  "devDependencies": {
    "eslint": "^1.0.0",
    "eslint-tester": "^0.7.0",
    "mocha": "^2.2.5"
  },
  "peerDependencies": {
    "eslint": ">=0.8.0"
  }
}
